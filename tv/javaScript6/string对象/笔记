【     *script("n")以字符n分割字符串返回一个数组，以空字符串分割会把字符串每个字符
都当作数组的一项内容，如果不存在n那么久会把整个字符串当做数组的内容  用，分割的
    也就是说n变成（，号）】把数组转成字符串

【join(n)加入某个字符n，用n连接数组的每项组成字符串，如果以空字符串连接那么
久不会加入新的内容  直接把数组的每项内容连接】把自负床转成数组




/*charAt  找到索引的位置
     *charCodeAt  找到索引位置上的编码值 要记的是A=65 a=97
      用是是assii码

     *indexOf("m")找m在字符串中第一次出现的值
     如果没找到 返回-1

     *lastIndexOf("m")找m在字符串中最后一次出现的值
     如果没找到 返回-1

     *substr(n,m):截取字符串：把字符串重n的位置开始截取，截取m个，如果只有一个参数n，
     那么就是从n开始截取到末尾

     substring(n,m):和上面不同的是 m的尾数不可获取

     *toLowerCase():把字符串中的字母装换成小写形式
      toUpperCase()                     大
    */

*公式
 求任意两个数之间的随机数
 Math.floor(Math.random()*(大数-小数+1)+小数)
